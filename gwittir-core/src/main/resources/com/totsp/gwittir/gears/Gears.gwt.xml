<?xml version="1.0" encoding="UTF-8"?>

<module>
	<inherits name='com.totsp.gwittir.Gwittir' />
    <inherits name='com.google.gwt.gears.Gears'/>
    <extend-property name="gwittir.windowcontext" values="gears" />
    <property-provider name="gwittir.windowcontext">
        <![CDATA[
            
	        if (typeof GearsFactory != "undefined") {
	        	window.__gwittir_storage = "gears";
	            return "gears";
	        }
	        try {
	          var gears = new ActiveXObject('Gears.Factory');
	          var db = gears.create('beta.database');
	          db.open('database-test');
	          alert("ActiveX");
	          window.__gwittir_storage = "gears";  	
	          return "gears";
	        } catch (e) {
	          if ((typeof navigator.mimeTypes != 'undefined')
	               && navigator.mimeTypes["application/x-googlegears"]) {
	            window.__gwittir_storage = "gears";
	            return "gears";
	          }
	        }
	        if(
             (navigator.userAgent.indexOf("Macintosh") != -1 &&  // trying to detect hosted mode here
             	navigator.userAgent.indexOf("WebKit") != -1)
              || navigator.userAgent.indexOf("Safari") != -1 ){
               window.__gwittir_storage = "html5";
        		return "html5";
        	}	
        	if(navigator.userAgent.indexOf("MSIE") != -1 ) {
	            window.__gwittir_storage = "userdata";
	        	return "userdata";
        	}
	    	var hasFlash = false;
		if (navigator.plugins && navigator.plugins.length){
			for(x in navigator.plugins ){
				if(navigator.plugins[x].name.indexOf("Shockwave Flash") != -1){
					hasFlash = true;
					break;
				}
			}
		} else if (navigator.mimeTypes && navigator.mimeTypes.length){
			x = navigator.mimeTypes['application/x-shockwave-flash'];
			if (x && x.enabledPlugin)
				hasFlash = true;
		} else if( window.ActiveXObject ){
			 if(eval("new ActiveXObject(\"ShockwaveFlash.ShockwaveFlash.9\");")){
			 	hasFlash = true;
			 } else if(eval("new ActiveXObject(\"ShockwaveFlash.ShockwaveFlash.10\");")){
			 	hasFlash = true;
			 }
		}
		if( hasFlash ) {
		    window.__gwittir_storage = "flash";
		    alert("flash");
			return "flash";
		}    
        
        
		if(window.globalStorage || window.localStorage){
		    window.__gwittir_storage = "domstorage";
		    alert("domstorage");
	    	return "domstorage";
	   	}
	   
	   	return "name";
	   	
        ]]>
    </property-provider>
    <replace-with
        class="com.totsp.gwittir.gears.client.util.impl.WindowContextPersisterGears">
        <when-type-is
            class="com.totsp.gwittir.client.util.impl.WindowContextPersister" />
        <when-property-is name="gwittir.windowcontext" value="gears"/>
    </replace-with>
</module>